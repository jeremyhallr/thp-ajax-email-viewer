<div id="email-view">
  <h2><%= @email.object %></h2>

  <p><%= @email.body %></p>

  <%= link_to "Supprimer", email_path(@email), :confirm => "Are you sure?", method: :delete, class: "btn btn-danger", remote: true %>
  <button class="btn btn-info">Marquer comme non-lu</button>
</div>